// import { createSlice } from "@reduxjs/toolkit";

// const initialState = {
//   markList: [],
// };

// const markSlice = createSlice({
//   name: "mark",
//   initialState,
//   reducers: {
//     setAsMark(state, action) {
//       state.markList.includes(action.payload)
//         ? state.markList.pop(action.payload)
//         : state.markList.push(action.payload);
//     },
//   },
// });

// export const { setAsMark } = markSlice.actions;

// export default markSlice.reducer;
